version: '2'
services:
    web:
        volumes:
            - .:/code
            - data:/data
        ports: 
            - "8000:8000"
        command: python manage.py runserver 0.0.0.0:8000
        environment: 
            DJANGO_SETTINGS_MODULE: 'xxicatalog.settings.dev'
